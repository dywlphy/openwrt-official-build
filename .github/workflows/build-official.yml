name: Build OpenWrt 24.10 Official

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'true'

permissions:
  contents: write

env:
  REPO_URL: https://github.com/openwrt/openwrt
  REPO_BRANCH: openwrt-24.10
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@main

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential clang flex bison g++ gawk gcc-multilib gettext \
          git libncurses-dev libssl-dev python3-pip python3-setuptools \
          rsync swig unzip zlib1g-dev file wget xsltproc \
          ecj java-propose-classpath libelf-dev libfuse-dev libjson-c-dev \
          liblzo2-dev liblzma-dev libzstd-dev quilt u-boot-tools \
          python3-dev python3-pyelftools python3-yaml ccache \
          aria2 curl subversion \
          libdbus-1-dev libexpat1-dev libglib2.0-dev \
          bash-completion
        sudo apt-get clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: Clone source code
      working-directory: /workdir
      run: |
        git clone -b ${{ env.REPO_BRANCH }} --single-branch --depth 1 ${{ env.REPO_URL }} openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: Cache OpenWrt dl (downloads)
      uses: actions/cache@v4
      with:
        path: /workdir/openwrt/dl
        key: openwrt-dl-${{ hashFiles('diy-part1.sh') }}-${{ github.run_id }}
        restore-keys: |
          openwrt-dl-${{ hashFiles('diy-part1.sh') }}-
          openwrt-dl-

    - name: Load custom feeds
      run: |
        chmod +x diy-part1.sh
        cd openwrt
        $GITHUB_WORKSPACE/diy-part1.sh

    - name: Cache OpenWrt build (staging_dir)
      uses: actions/cache@v4
      with:
        path: |
          /workdir/openwrt/staging_dir
          /workdir/openwrt/build_dir
        key: openwrt-build-${{ hashFiles('config.txt') }}-${{ github.run_id }}
        restore-keys: |
          openwrt-build-${{ hashFiles('config.txt') }}-
          openwrt-build-

    - name: Update feeds
      run: cd openwrt && ./scripts/feeds update -a

    - name: Install feeds
      run: cd openwrt && ./scripts/feeds install -a

    - name: Load custom configuration
      run: |
        [ -e config.txt ] && cp config.txt openwrt/.config
        chmod +x diy-part2.sh
        cd openwrt
        $GITHUB_WORKSPACE/diy-part2.sh

    - name: SSH connection to Actions
      uses: P3TERX/ssh2actions@v1.0.0
      if: github.event.inputs.ssh == 'true' || github.event.action == 'ssh'

    - name: Clean Go mod cache
      run: |
        cd openwrt
        rm -rf dl/go-mod-cache
        make package/tailscale/clean 2>/dev/null || true

    - name: Download package
      run: |
        cd openwrt
        make defconfig
        # 强制启用可能被defconfig禁用的包
        echo "CONFIG_PACKAGE_cups-pdf=y" >> .config
        echo "CONFIG_PACKAGE_frp=y" >> .config
        make defconfig
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile the firmware
      id: compile
      run: |
        cd openwrt
        
        # 开始编译
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> "$GITHUB_OUTPUT"
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> "$GITHUB_ENV"
        echo "FILE_DATE=_$(date +%Y%m%d%H%M)" >> "$GITHUB_ENV"

    - name: Upload firmware
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin/targets/*/*

    - name: Create Tag
      if: steps.compile.outputs.status == 'success'
      run: |
        git config --local user.name "GitHub Actions"
        git config --local user.email "actions@github.com"
        TAG_NAME="v24.10+Avahi+功能包-$(date +%Y%m%d)"
        git tag -a "$TAG_NAME" -m "OpenWrt 24.10 含Avahi/Tailscale/ACME/文件管理器/CUPS-PDF/访问控制 $(date +%Y.%m.%d)"
        git push origin "$TAG_NAME"
